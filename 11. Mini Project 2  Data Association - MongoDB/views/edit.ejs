<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <header class="bg-gray-900 p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="#" class="text-white text-lg font-bold">My Website</a>
            <nav class="flex space-x-4">
                <a href="logout" class=":text-gray-300 disable">Logout</a>
            </nav>
        </div>
    </header>

    <div class="container mx-auto py-10 flex justify-center">
        <div class="w-full max-w-md bg-gray-800 rounded-lg shadow-md p-6">
            <h2 class="text-2xl font-semibold text-white mb-4">Edit Post</h2>
            <form action="/update/<%= post._id%>" method="POST">
                <label for="postContent" class="block text-sm font-medium mb-2">Post Content</label>
                <textarea id="postContent" name="content" rows="6"
                    class="w-full p-3 rounded bg-gray-700 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-500 resize-none"
                    placeholder="Update your post content here..." name="content"><%= post.content%></textarea>
                
                <div class="mt-4 flex justify-between">
                    <button type="submit"
                        class="bg-gray-700 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Save Changes</button>
                    <a href="/profile" class="text-gray-400 hover:text-gray-300">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>
